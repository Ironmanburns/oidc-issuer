# oidc-issuer

Public OIDC discovery + JWKS for local kind cluster Azure Workload Identity federation.

**Issuer:** `https://ironmanburns.github.io/oidc-issuer`

Keys are synced from the kind cluster (public verification keys only). Private signing keys never leave the cluster.
